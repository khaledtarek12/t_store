enum TextSizes { small, meduim, large }

enum OrderStatus { processing, shiped, delivered }

enum PaymentMethods {
  paypal,
  googlePlay,
  appelPaly,
  visa,
  masterCard,
  creditCard,
  payStack,
  razorPay,
  paytm
}
